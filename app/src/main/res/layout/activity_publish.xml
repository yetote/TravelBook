<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.tf.travelbook.PublishActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/publish_toolbar"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="?attr/colorPrimary"
        android:fitsSystemWindows="true"
        app:title="发表"
        app:titleTextColor="@color/white" />

    <EditText
        android:id="@+id/publish_content"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_alignStart="@+id/publish_sure"
        android:layout_below="@+id/publish_toolbar"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:gravity="top"
        android:hint="请输入内容" />

    <Button
        android:id="@+id/publish_sure"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/publish_content"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:background="@color/colorPrimary"
        android:text="发布"
        android:textColor="@color/white" />

    <utils.RevelBackground
        android:id="@+id/publish_revel"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</RelativeLayout>
